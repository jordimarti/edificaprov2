<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : t('.titol_pagina') %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <script src="https://content.jwplatform.com/libraries/sERzAduS.js"></script>
    
    <%= csrf_meta_tags %>
    <%= yield(:stripe_scripts) %>
  </head>

  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <main>
      <%= yield %>
    </main>

    <footer>
    <%= render 'layouts/footer' %>
    </footer>
  </body>
</html>
